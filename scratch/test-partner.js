require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: profile } = await supabase.from('profiles').select('*').eq('partner_share_token', '0f4f777ca4b72673b611b81cff253a1f').single(); console.log('Profile ID:', profile.id); const [logsRes, onboardRes] = await Promise.all([ supabase.from('cycle_logs').select('*').eq('user_id', profile.id).order('period_start', { ascending: false }).limit(6), supabase.from('onboarding_data').select('*').eq('user_id', profile.id).maybeSingle() ]); console.log('Logs:', logsRes.data); console.log('Onboard:', onboardRes.data); } run().catch(console.error);
